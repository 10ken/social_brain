rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Attachment sync is disabled alongside Firestore record sync. The active
    // app stores capture attachments in device-encrypted local files instead.
    match /users/{userId}/attachments/{attachmentId} {
      allow read, write: if false;
    }
  }
}
